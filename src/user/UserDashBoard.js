import React from "react";
import Base from "../core/Base";

const UserDashboard = () => {
  return (
    <Base>
      <h2>This is UserDashboard</h2>
    </Base>
  );
};

export default UserDashboard;
